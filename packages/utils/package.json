{
  "name": "@mstack/utils",
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "svelte": "./src/index.ts"
    }
  },
  "files": [
    "!dist/**/*.spec.*",
    "!dist/**/*.test.*",
    "dist"
  ],
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "prettier --write . && eslint --fix .",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@mstack/typescript-config": "workspace:*"
  }
}
